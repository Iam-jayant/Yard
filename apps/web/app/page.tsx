// apps/web/app/page.tsx
// Terminal landing page — the first thing any user sees
// Full spec in docs/PHASE-1.md § Terminal Landing Experience

export default function HomePage() {
  return (
    <main>
      {/* TerminalLoader component goes here */}
      {/* City map fades in after terminal sequence */}
    </main>
  );
}
